import { Meta, Primary, Controls } from '@storybook/addon-docs/blocks';
import * as RadioGroupStories from './pao-radio-group.stories';

<Meta of={RadioGroupStories} />

# Radio Group

A fieldset wrapper that coordinates a set of `pao-radio` elements.

## Interactive Demo

<Primary />

## Properties

<Controls />

## Events

- `paoChange`: Fired when a child radio is selected. `event.detail = { value: string }`
