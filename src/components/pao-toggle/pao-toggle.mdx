import { Meta, Primary, Controls } from '@storybook/addon-docs/blocks';
import * as ToggleStories from './pao-toggle.stories';

<Meta of={ToggleStories} />

# Toggle

A binary on/off switch control. Semantically equivalent to a checkbox but visually distinct — use for settings and immediate-effect actions.

## Interactive Demo

<Primary />

## Properties

<Controls />

## Events

- `paoChange`: Fired on change. `event.detail = { value: string, checked: boolean }`
