import { Meta, Story, Primary, Controls } from '@storybook/addon-docs/blocks';
import * as ButtonGroupStories from './pao-button-group.stories';

<Meta of={ButtonGroupStories} />

# Button Group

A component that groups multiple buttons together with various selection modes and styling options.

## Interactive Demo

<Primary />

## Properties

<Controls />

## Events

- `pao-selection-change`: Fired on every button click. `event.detail = { active: number[], clickedIndex: number }`
- `pao-active-change`: Fired when the active selection updates. `event.detail = { active: number[] }`
